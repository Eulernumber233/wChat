"""Generated gRPC stubs for wChat AgentServer.

Do not edit files in this directory by hand. They are regenerated by
`proto/generate_pb.bat`.

The `sys.path` shim below lets the generated `message_pb2_grpc.py` find
`message_pb2.py` via its default top-level import, since the files were
produced with `--python_out` on a flat output directory.
"""
import os
import sys

sys.path.insert(0, os.path.dirname(__file__))
